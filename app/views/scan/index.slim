div class="container"
    div class="panel panel-default"
      div class="panel-heading" 
        h3 class="panel-title" Requested Scans
      div class="panel-body"
       table class="table table-striped"
         thead
           tr
             th #
             th Address
             th Requestor
         tbody
           - @scans.each do |s|
             tr
               td 
                 = s.id
               td 
                 = link_to Address.find(s.targets).location, s
               td 
                 = link_to user.name, user_path(@current_user.id)
